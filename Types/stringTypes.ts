let firstName: string = 'Muhsina';

let multiLine = `this is a 
multiple line
string`;

// string interpolation
let goodGirl = `${firstName} is a very good girl`;

console.log(multiLine);
console.log(goodGirl);
