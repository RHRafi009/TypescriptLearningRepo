let names: string[] = [];
names[0] = 'Muhsina';
names.push('Protity');

console.log(names);

let namesLength = names.length;

names.forEach(n => console.log(n));

let scores: (string | number)[];
scores = ['Two', 2, 'Plus', -4];
console.log(scores);